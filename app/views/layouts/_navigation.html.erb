<!-- Navigation -->
<nav class="navbar navbar-default navbar-custom navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header page-scroll">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          Menu <i class="fa fa-bars"></i>
      </button>
      <%= link_to 'IHM Music', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if params[:controller] == 'posts' && params[:action] == 'index'%>">
          <%= link_to 'Updates', posts_path %>
        </li>
        <li class="<%= 'active' if params[:action] == 'calendar' %>">
          <%= link_to 'Calendar', calendar_path %>
        </li>
        <li class="<%= 'active' if params[:action] == 'documents' %>">
          <%= link_to 'Documents', documents_path unless current_user.nil? %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'users' %>">
          <%= link_to 'Users', users_path unless current_user.nil? %>
        </li>
        <li>
          <% if current_user.nil? && params['action'] != 'new' %>
            <%= link_to "Log in", new_session_path(User.new), class: 'sign-up'  %>
          <% else %>
            <%= link_to "Log out", destroy_session_path(User), method: :delete, class: 'sign-up' %>
          <% end %>
        </li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>
